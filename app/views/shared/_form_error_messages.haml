- if @user.errors.any?
  #error_explanation
    .alert.alert-error
      = "Oops! There are #{pluralize(@user.errors.count, "error")}."
    %ul
      - @user.errors.full_messages.each do |msg|
        %li.text-error=msg
