- if object.errors.any?
  #error_explanation
    .alert.alert-error
      = "Oops! There's #{pluralize(object.errors.count, "error")}."
    %ul
      - object.errors.full_messages.each do |msg|
        %li.text-error=msg
